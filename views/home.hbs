<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Secret Chat</title>
<style>
body {
  font-family: 'Poppins', sans-serif;
  background: linear-gradient(135deg, #1e3c72, #2a5298, #ff9a9e, #fad0c4, #fad0c4, #ffdde1);
  background-size: 600% 600%;
  animation: gradientBG 12s ease infinite;
  margin: 0;
  padding: 0;
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  color: #fff;
  text-align: center;
}

@keyframes gradientBG {
  0% { background-position: 0% 50%; }
  25% { background-position: 50% 100%; }
  50% { background-position: 100% 50%; }
  75% { background-position: 50% 0%; }
  100% { background-position: 0% 50%; }
}

.container {
  max-width: 600px;
  background: rgba(255, 255, 255, 0.15);
  backdrop-filter: blur(15px);
  padding: 40px;
  border-radius: 15px;
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
  border: 2px solid rgba(255, 255, 255, 0.3);
}

h1 {
  font-size: 40px;
  font-weight: bold;
  margin-bottom: 20px;
  text-transform: uppercase;
  letter-spacing: 2px;
  background: linear-gradient(45deg, #ffdde1, #1e3c72);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

p {
  font-size: 18px;
  font-weight: 300;
  margin-bottom: 25px;
}

.secret-button {
  display: inline-block;
  padding: 15px 35px;
  border: none;
  background: linear-gradient(45deg, #ff416c, #ff4b2b, #1e3c72);
  color: #fff;
  text-decoration: none;
  border-radius: 50px;
  font-weight: bold;
  font-size: 18px;
  transition: 0.3s;
  box-shadow: 0 5px 15px rgba(255, 75, 43, 0.5);
  cursor: pointer;
}

.secret-button:hover {
  transform: translateY(-3px);
  box-shadow: 0 8px 20px rgba(255, 75, 43, 0.7);
}

.down-arrow {
  display: block;
  font-size: 30px;
  margin-top: 10px;
  animation: bounce 1.5s infinite;
}

@keyframes bounce {
  0%, 20%, 50%, 80%, 100% {
    transform: translateY(0);
  }
  40% {
    transform: translateY(-10px);
  }
  60% {
    transform: translateY(-5px);
  }
}

@media (max-width: 768px) {
  .container {
    padding: 30px;
  }
  h1 {
    font-size: 30px;
  }
  p {
    font-size: 16px;
  }
  .secret-button {
    font-size: 16px;
    padding: 12px 25px;
  }
  .down-arrow {
    font-size: 24px;
  }
}
</style>
</head>
<body>
<div class="container">
  <h1>Welcome to the Secret Chat</h1>
  <p>Developed by Naveen GR</p>
  <p>Click the button below to access the secret chat room</p>
  <span class="down-arrow">&#9660;</span>
  <a href="/secret-chat" class="secret-button">Click here for a secret chat</a>
</div>
</body>
</html>
